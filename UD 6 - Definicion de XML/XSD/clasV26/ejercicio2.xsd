<!--
    Escribir el contenido del archivo "ejercicio2.xsd" que permita validarlo, teniendo en cuenta que el elemento "clave" 
    debe poder tomar por valor un mínimo de cuatro caracteres y un máximo de diez. 
    Dichos caracteres pueden ser indistintamente letras mayúsculas o minúsculas de la "a" a la "z", 
    o dígitos del "0" al "9". La restricción solamente podrá aplicarse al elemento "clave".

Para ello, se debe utilizar xs:pattern y también:

    xs:minLength que permite especificar la longitud mínima.
    xs:maxLength que permite especificar la longitud máxima.

-->